// @nori/ui — Phase 1 placeholder.
//
// Phase 2 will extract the Soft Spectrum design tokens (colors, gradients, radii, spacing,
// shadows, motion) and components (AppShell, Sidebar, PersonaShelf, PersonaPill,
// JourneyViewSwitch, RunCard, etc.) from prototype/ (Journey Atlas) into this package.
//
// This is scaffolded as a plain TypeScript package for now, with no react dependency, to
// avoid pinning a React version before Phase 2 decides how components are built. react will
// be added as a dependency then.
export {};
